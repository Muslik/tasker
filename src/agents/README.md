# Agents

CLI provider adapters, stream parsing, profiles, skills, and usage/cost data.

Depends on graph/planning contracts, workspace harness materialization, steps,
shared values, and harness contracts.

Exposes provider contracts and CLI implementations.
