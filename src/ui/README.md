# Operator UI

Fresh operator surface for the rebuild.

- TanStack Query owns server state.
- `realtime.ts` is the only background refresh path.
- API modules validate the shared control-plane contracts.
- Components stay small and expose explicit extension points.
