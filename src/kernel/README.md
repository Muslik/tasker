# Kernel

Temporal workflows, clients, worker bootstrap, and execution contracts.

Depends on shared contracts and the graph, planning, and step surfaces that
the existing workflow runtime currently wires together.

Exposes the Temporal worker/client entry points and workflow implementations.
