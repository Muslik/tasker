# Workspace

Git worktrees, repository catalog, Docker runtime, and harness pack
materialization.

Depends on agents, graph schemas, harness, store, and shared infrastructure.

Exposes workspace and managed-repository contracts and implementations.
