# Руководство для AI-агента — OneTwoTrip Avia

**Читай выборочно**: открывай только доки под текущую задачу — не вычитывай всё заранее.

## Понять проект (`docs/`)
- `project.md` — домен авиа-вертикали
- `architecture.md` — FSD: слои, импорты, стек
- `conventions.md` — нейминг, TS-стиль, порядок в компоненте

## Реализация → скиллы (implement, грузятся по триггеру)
`ui-kit` — компоненты `@ott/ui` + авторинг UI · `state-data` — Redux/API/состояние · `tracking` — аналитика · `localization` — переводы.
<ui-kit-first>Перед написанием UI СНАЧАЛА ищи готовый компонент в `@ott/ui`.</ui-kit-first>

## Ревью
- `REVIEW.md` — гайдлайны код-ревью

## Запустить / дойти до состояния
- `app-runbook.md` — как поднять dev-сервер, логин, карта состояний (deep-link/API-моки).
  Нужен скиллам `playwright-demo` и `reproduce` (демо-видео, воспроизведение бага).

## Коммиты

Одна строка, без тела. **Никаких подписей и трейлеров** — ни `Co-Authored-By`,
ни «Generated with …», ни упоминаний инструмента.

Формат: `TICKET-123: краткое описание` (ключ задачи — из ветки). Допустим тип:
`TICKET-123: fix: ...`, `TICKET-123: chore: ...`.

За референсом стиля — последние коммиты репозитория: `git log --oneline -20`.

<!-- wt-guide -->
---

## Worktree

Worktree создаются и удаляются через `wt`:

- `wt <ветка>` — подключить worktree к **существующей** ветке (локальной или с origin).
  Ветки нет → ошибка с подсказкой, новая НЕ создаётся.
- `wt -b <ветка> [--base <ref>]` — создать **новую** ветку + worktree.
- `wt` — выбрать существующую ветку через fzf.
- `wt cd [ветка]` — перейти в worktree (без аргумента — fzf).
- `wt list` — worktree текущего проекта.
- `wt rm <ветка> [--force]` — удалить worktree.
