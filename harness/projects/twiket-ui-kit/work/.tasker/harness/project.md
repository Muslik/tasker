# ui-kit workflow notes

- Copy changes use the external translation workflow.
- Run `pnpm translations:extract` before waiting for translators.
- Pull completed translations with `pnpm translations:pull`.
- Packages below `packages/@ott/` use the human-owned final publication boundary.
